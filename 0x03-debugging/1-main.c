#include <stdio.h>

/**
* main - causes an infinite loop
* Return: 0
*/

int main(void)
{
	int i;
	/* print statement */
	printf("Infinite loop incoming :(\n");
	/* value assignment */
	i = 0;
	/*
	 * while (i < 10)
	 * {
	 * putchar(i);
	 * }
	 */
	printf("Infinite loop avoided! \\o/\n");
	/* return always 0 */
	return (0);
}
