#include <stdio.h>

/**
 * main - function to print in stdout
 * Return: Always 0.
 */
int main(void)
{

	/*String initialization*/
	char prog[] = "\"Programming is like building a multilingual puzzle";

	puts(prog);

	return (0);
}
